 export const sum = (a,b) => a + b; 
 const sub = (a,b) => a - b;  
 const mul = (a,b) => a * b;  
const div = (a,b) => a / b;  

//modules.exports = {sum,sub,mul,div}  //exporting the functions
// The above code is the content of utils.js file. It contains the functions that we want to use in our index.js file. We are exporting these functions so that we can use them in our index.js file.


//**********now we learn import funcction*********** */
